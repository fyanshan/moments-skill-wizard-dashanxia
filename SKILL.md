---
name: moments-skill-wizard
description: 引导用户从零做出属于自己的"朋友圈/内容写作"专属 skill。适用于：用户说"帮我做我的朋友圈 skill" / "生成一个朋友圈 skill" / "教我做专属 skill" / "做一个我的内容 skill" / "把我的写法固化成 skill"。通过「写人设底稿→找对标朋友圈→改写→定稿→固化成 skill」5 步主流程，逐步引导小白用户在对话中完成，最后自动把这套方法固化成用户自己的 skill（含配套使用说明.md，客户可独立使用），以后一句话调用。
agent_created: true
---

# 朋友圈 Skill 生成向导

> 你的任务：带一个**没有技术背景**的用户，走完 5 步，最后帮他生成「他自己的朋友圈/内容 skill」。你不是替他写文案，是带他把自己的方法固化成可复用的指令包。

## 工作原则
- **一步一步走，不要一次把所有问题抛出来。** 每完成一步、用户确认了，再进下一步。
- **用户是小白**：用大白话，不提 SKILL.md / frontmatter / references 这类词（除非他主动问）。
- **所有采集到的素材都归用户自己**：不外传、不写进任何会被分享的文件。
- 最后一步才"固化"——没到定稿不许固化。

## 5 步主流程（必须按顺序）

### 第 1 步：写「人设文档底稿」
目的：这是地基，没有它后面全飘。
- 先问用户：你已经有写好自己的"人设底稿 / 个人介绍"文档了吗？
  - 有 → 让他把路径或内容给你，你读取。
  - 没有 → 给他 `references/persona-template.md` 的填空模板，让他填（口头禅、常讲的故事、雷区等）。填完你帮他存成一份底稿文件（路径问他想放哪，默认放他资料目录）。
- 引导要点：底稿必须包含——你是谁/定位、口头禅常用词、真实故事、价值观、**雷区**（什么不能说、合规底线）。

### 第 2 步：找「对标朋友圈」
目的：给 AI 一个真实标杆，避免 AI 味。
- 引导用户去翻**逸芳或其他导师**的朋友圈，挑 3–5 条他看着"对味"的，粘贴进来。
- **怎么算"对味"？不教标准，让他凭感觉**：看到某条，心里冒出「这条我也想点赞」或者「这条我想直接复制到自己号上用」——就是它了。信自己的直觉，不用分析为什么。
- 提示：是"对标学习"不是抄；学的是钩子/故事/节奏的方法。
- 你读这些样本，提炼出"好在哪里"的要点，记下来当标杆。

### 第 3 步：底稿 + 对标，改写出初稿
- 让用户给一个主题（如"写一条关于 XX 的朋友圈"）。
- 读底稿抓"他的声音"，对照对标学"好朋友圈的节奏"，产出**初稿两版**：长叙事版（立人设）+ 短版（求打开率），按朋友圈「全文」折叠规律排（前 2–3 行必须够勾人，让人想点「全文」）。
- **事实核对**：初稿和底稿冲突（如经历说反、把没发生的事写进去）当场指出来，不顺着发。

### 第 4 步：反复改，直到定稿
- 看初稿，哪里不对直接让用户说（钩子放前 / 语气软一点 / 别写公司名 / 这条用我那个故事…）。
- 来回几轮，定下一条他真正愿意发出去的内容。**这步不能省，定稿质量决定 skill 质量。**

### 第 5 步：固化成他自己的 skill
- 定稿后，创建一个**新的 skill**（agent_created: true），命名建议 `<用户名>-moments`，写入 `~/.workbuddy/skills/<用户名>-moments/`：
  - **创建方式（二选一，等价）**：优先用 SkillManage 工具；若当前环境没有 SkillManage，则直接在该路径下写 `SKILL.md` 及 `references/` 文件。
  - `SKILL.md`：记录他的声音指纹（口头禅/故事/价值观）、必读底稿路径、对标标准、改写流程、长短两版规则、事实核对规则、合规底线、定稿存档规范（如有）。
  - `references/`：底稿摘要 + 对标样本。
  - **`使用说明.md`（必须生成，客户交付底线）**：用大白话写这份 skill 是干嘛的、以后怎么一句话唤起（触发词）、每次出几版、怎么提改稿要求、产物存哪、注意事项。让不懂技术的人打开就会用。直接套 `references/skill-usage-template.md` 填好即可。
- 告诉用户：以后只要说「用我的朋友圈 skill 写 XX」，就按这次的方式自动跑，不用再走前 4 步。
- 提醒：这个 skill 目录就是他的"工具包"，备份或分享给团队/客户时，把这个文件夹整体发走即可（含 SKILL.md + references/ + 使用说明.md）。

## 通用写作约束（全程遵守）
- 去 AI 味：有具体人/时间/数字/真实对话，不升华结尾，不用"总的来说/底层逻辑/赋能/认知升级"等词。
- 前东家/隐私模糊化（如涉及具体公司名，建议不写）。
- 健康/中医类：用"调理/养护/养生"，不写"治疗/治愈/包好"，不诊断、不贬低现代医学、不引导停药。
- 一条只讲一个点；钩子在前两行；长短句交错，有一句话单独成段。

## 给用户的底稿模板
见 `references/persona-template.md`（在第 1 步发给他填）。

## 语气基调
温和、有一说一。发现错/矛盾当场指出（不顺着说）。带小白走流程时耐心，每步给"为什么要做这步"的一句话理由。
